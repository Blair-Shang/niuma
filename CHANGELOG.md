# Changelog

只记录用户可感知的功能变化，不记录技术栈与实现细节。
每个版本按 **新增** / **修复** / **优化** 分组，没有内容的分组不写。

---

## [Unreleased]

## [1.0.8] - 2026-09-03

### 新增

- 打开已连接的 SSH 页签时，AI 助手可查看远程目录、文件、主机指标与进程，并在确认后执行短命令；使用当前会话，不必另连
- 可将工作区页签（含 SSH 终端）拖进 AI 助手，作为对话引用

### 修复

- 应用内更新后不再丢掉桌面云账号登录

### 优化

- 检查更新后的安装改为静默覆盖，不再弹出安装向导

## [1.0.7] - 2026-09-02

### 修复

- 查询结果里打开的大字段窗口拖到左侧连接树不再透底
- 切换主题后大字段窗口内容不再空白，不必再点 JSON / XML 才出来

### 优化

- 启动完成后发现新版本会弹出更新说明；检查失败或已是最新则不打扰

## [1.0.6] - 2026-09-02

### 新增

- API 集合、环境和当前环境选择写入本机，重启后仍在；侧栏可建文件夹并导入导出请求文档（不含连接密码）
- API 侧栏可查看发送历史（本机单独保存，导入导出不含历史）
- API 能力服务可监听或发送 TCP / UDP（原始套接字）
- API 工作台发送走本机套接字服务：TCP / UDP 原帧，明文 HTTP 经 TCP；不再用演示响应
- API 工作台 TCP / UDP 每个请求页签独立收发，可同时开多个；`0.0.0.0:端口` 为监听，关掉标签才断开

### 修复

- API 工作台 UDP 服务端回复发到最近（或点选的）对端，不再把 `0.0.0.0` 监听地址当成目的；绑定指定网卡时也落在该地址而不是临时端口

### 优化

- 源码按 Apache License 2.0 开源；安装包附带 LICENSE / NOTICE，安装声明不再写成「未开源」
- Windows 安装程序默认装到当前用户目录，不再先要管理员权限
- 安装向导免责声明写明：开发者对使用后果不承担责任，免费获得时责任上限为零
- 帮助里的更新日志只展示最近三个版本，每条显示完整说明
- AI 助手查已打开的库时走当前连接，不必再单独启动 Vastbase 只读 MCP
- API 侧栏新建请求先填写名称，确认后才写入集合；取消不落库
- API 套接字客户端收发流与发送坞可上下拖拽分栏，按工作区高度调整
- API 套接字发送坞改用与 HTTP Body 相同的代码编辑器；Ctrl+Enter 发送
- API 页签标题用请求名称，不再在名称前再拼一遍方法（避免「TCP TCP 服务端」）
- API 页签悬浮提示标明协议与客户端/服务端，同名请求也能区分
- API 套接字服务端接入列表与收发流可左右拖、发送坞可上下拖；空态不再并排两个空心圆
- API 套接字连接失败时顶栏只显示「连接失败」，完整原因在下方单独一条，不再重复挤进状态行
- API 套接字发送后清空输入框
- API 套接字 Hex / Base64 按业界只解读已编码输入，不再把原文偷偷编过去
- API 套接字工作台只显示当前协议（TCP 或 UDP），页内不再互相切换

## [1.0.5] - 2026-08-30

### 修复

- 首次打开数据库、Redis 或 SSH 时，其它连接和终端不再跟着卡住几十秒
- 退出客户端后不再残留后台的数据库 / SSH 进程；异常退出后也不会再自动拉起一份
- 新建或删除文件夹会立刻写入，失败会提示并还原；不再缓存在浏览器本地
- 展开或拖拽后再改文件夹，重启后结构还在；删除文件夹后连接回到上一层或根目录

### 新增

- 登录后可使用系统提供的 AI 助手，无需自己填写 API Key；仍可在设置里接入自己的模型

### 优化

- 发现新版本后在后台下载；关掉更新窗口不影响。下好后状态栏可「重启以更新」
- 展开折叠文件夹更跟手；导入一批连接时侧栏一次更新完

## [1.0.4] - 2026-08-30

### 新增

- 邮箱验证码登录：未注册的邮箱验证通过后自动创建账号
- Linux / macOS 也可在应用内下载并拉起安装包，与 Windows 同一套自动检查更新

### 优化

- 帮助菜单更新日志展示历史版本，并用 Markdown 渲染（读云端已发布列表）

## [1.0.3] - 2026-08-30

### 修复

- Windows / Linux / macOS 安装包能正常打出；安装后界面与本机一致

## [1.0.2] - 2026-08-30

### 修复

- 同一层级新建文件夹重名时自动加序号（新建文件夹、新建文件夹1、新建文件夹2）
- 移动连接或删除文件夹时，其它文件夹不再被连带展开
- 展开数据库后能再次看到表 / 视图等分类（懒加载结果被父节点旧缓存挡住）
- 正式安装包右键不再出现返回、前进、打印、查看网页源代码

### 优化

- Linux / macOS 安装包按 **beta** 预览渠道发布到 cloud；Windows 仍为正式版 stable
- 侧栏新建文件夹只插入新节点，不再整棵连接树刷新
- 侧栏文件夹展开 / 折叠会记住，重启后保持原样

## [1.0.1] - 2026-08-29

### 修复

- Linux 安装后能正确加载旁边的 Chromium 运行库
- macOS 安装包补齐 Helper 子进程，避免窗口起不来
- Windows 安装后补齐 Chromium 资源文件，避免启动界面异常
- 设置中下载外部工具、导出文件不再被拦截

### 优化

- 安装包文件名带系统名，例如 `NiuMa-1.0.1-windows-x64-Setup.exe`；发布页只提供安装包，不再单独挂内部服务程序
- 正式安装包不附带前端源码映射；安装后无法用 F12 打开开发者工具

## [1.0.0] - 2026-08-28

### 新增

- 桌面客户端：Windows 绿色目录与安装程序；Linux、麒麟、macOS 安装包
- 应用内检查并安装更新
- 运维连接树，统一管理主机与数据源
- FTP / FTPS：站点连接、浏览与传输
- SSH 远程终端；SFTP 文件浏览与在线编辑
- 文件工作台：多标签查看与编辑，各模块共用
- 数据库连接与管理：Redis、MongoDB、MySQL、MariaDB、SQLite、PostgreSQL、Vastbase、人大金仓、SQL Server、ClickHouse、达梦、Oracle
- SQL 方言补全；SQLite 表设计、备份与导入导出
- 侧栏 AI 助手，可配置模型与外部工具
- 设置中检测并配置外部命令行工具
